version: '3.7'
services:
  web:
    build: .
    container_name: infoUsuario_app
    restart: unless-stopped
    ports:
      - "1090:80"
    networks:
      - infoUsuario_app-network

networks:
  infoUsuario_app-network:
    driver: bridge  